{
	"version": 2,
	"builds": [
		{ "src": "package.json", "use": "@vercel/svelte" },
		{ "src": "server/api/index.py", "use": "@vercel/python" }
	],
	"routes": [
		{ "src": "/api/(.*)", "dest": "server/api/index.py" },
		{ "src": "/(.*)", "dest": "/" }
	]
}
